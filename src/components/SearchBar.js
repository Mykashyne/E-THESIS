import React from 'react';

function SearchBar() {
  return (
    <div>
      <input type="text" placeholder="Search thesis..." />
      <button>Search</button>
    </div>
  );
}

export default SearchBar;
